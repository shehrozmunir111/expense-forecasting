# Web framework
fastapi==0.111.1
uvicorn[standard]==0.30.1

# Database
sqlalchemy==2.0.30
psycopg[binary]==3.2.9

# Config & validation
pydantic==2.7.4
pydantic-settings==2.3.4

# LLM providers (install both; active one selected via LLM_PROVIDER env var)
# Bumped from 0.28.0 / 1.35.3 to versions required by the langchain-* wrappers.
# The categorization service uses stable client APIs that work on these versions.
anthropic==0.109.2
openai==2.41.1

# Conversational agent: Adaptive RAG + memory ("Chat with your finances")
# LLM is provider-agnostic; .env points these at a local LM Studio (OpenAI-compatible)
# server by default, but cloud OpenAI / Anthropic / Gemini are supported too.
langgraph==1.2.5
langchain-core==1.4.7
langchain-openai==1.3.2          # also used for LM Studio (OpenAI-compatible) + nomic embeddings
langchain-anthropic==1.4.6       # optional "show" provider
langchain-chroma==1.1.0          # vector store wrapper
chromadb==1.5.9
langgraph-checkpoint-sqlite==3.1.0    # conversation memory (dev / SQLite profile)
langgraph-checkpoint-postgres==3.1.0  # conversation memory (PostgreSQL profile)
# Optional: Gemini "show" provider. Lazily imported; install only if used.
# langchain-google-genai==2.1.0

# ML / data
scikit-learn==1.5.0
numpy==1.26.4
pandas==2.2.2
joblib==1.4.2

# HTTP (for seed script)
httpx==0.27.0

# Forms / multipart
python-multipart==0.0.9

# Testing
pytest==8.2.2
pytest-asyncio==0.23.7
